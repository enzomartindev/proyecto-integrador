import { Box } from "@mui/material";
import "./logo.scss";

const Logo = () => {
    return (
        <Box className="logo">
            <img
                src="/images/layout/logo2.jpeg"
                alt="Logo de la tienda"/>
        </Box>
    );
};

export default Logo;